# CHANGELOG_AI

- **2026-05-01:** Инициализирована память проекта в `docs/ai/` (обзор, архитектура по коду и Docker, текущее состояние, решения по процессу AI, конвенции).
- **2026-05-01:** SEO: `robots.txt`, `sitemap.xml`, централизованные meta/canonical/keywords по маршрутам (`routeSeo.ts`, `AppRouteSEO`), JSON-LD и `hreflang` в `SEO.tsx`, правки `index.html` и заголовков; сборка запускает `vitest run`; исправлен дубликат `getAiHistory` в `api/client.ts`.
